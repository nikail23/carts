// Пример демонстрации работы проекции на повернутую землю

/*
Алгоритм работы _projectToGround с учетом поворота земли:

Пример 1: Земля повернута на 45 градусов вокруг X оси
```
Ground rotation: new Quaternion().setFromAxisAngle(new Vector3(1, 0, 0), Math.PI / 4)

1. Локальная нормаль земли: (0, 1, 0) - "вверх" в локальных координатах
2. Применяем поворот земли: 
   groundNormal = (0, 1, 0).applyQuaternion(groundQuaternion)
   Результат: (0, cos(45°), sin(45°)) ≈ (0, 0.707, 0.707)
3. Направление рейкастинга: -(0, 0.707, 0.707) = (0, -0.707, -0.707)
4. Луч будет направлен под углом 45° к горизонту
```

Пример 2: Земля повернута на 30 градусов вокруг Z оси  
```
Ground rotation: new Quaternion().setFromAxisAngle(new Vector3(0, 0, 1), Math.PI / 6)

1. Локальная нормаль: (0, 1, 0)
2. После применения поворота: (-sin(30°), cos(30°), 0) ≈ (-0.5, 0.866, 0)
3. Направление рейкастинга: (0.5, -0.866, 0)
```

Преимущества такого подхода:
- Следы всегда ложатся точно на поверхность земли
- Работает для любых углов поворота земли
- Автоматически адаптируется к изменениям ориентации земли
- Fallback на математическую проекцию если рейкастинг не сработал

Применение:
```typescript
// Создание WheelTireMark с учетом поворота земли
const wheelTireMark = new WheelTireMark(wheelMesh, 500, groundMesh);

// При вызове addTireMark автоматически происходит проекция
wheelTireMark.addTireMark(wheelWorldPosition, wheelRotation);

// Позиция следа будет точно на поверхности земли, 
// даже если земля наклонена
```
*/

export {};
